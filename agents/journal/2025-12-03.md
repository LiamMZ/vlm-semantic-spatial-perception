### 2025-12-03-1631 – Merged registry predicates + dual analyzer prompts

- **Owner**: AI Agent (merge + prompt switch)
- **Agents affected**: LLMTaskAnalyzer, PDDLDomainMaintainer, TaskOrchestrator, OrchestratorConfig, prompts
- **Git commit(s)**: Based on 51801755e9a5d743e303c0d5eccc8659686ccec6
- **Context**: Pulled `lmh/object-registry-updates` into a new merge branch while keeping quantificational goal handling and exposing both prompt variants for the task analyzer.
- **Actions**:
  - Merged registry predicate tracking, global predicate handling, and object snapshot updates from the lmh branch into `merge-registry-tamp-analyzer`.
  - Added `config/llm_task_analyzer_prompts_lmh.yaml` and a `task_analyzer_prompts_path` knob in `OrchestratorConfig` plumbed through `PDDLDomainMaintainer` to `LLMTaskAnalyzer` for prompt selection.
  - Ensured saved PDDL files are written after refreshing goals from task analysis while retaining predicate tracking defaults.
- **Result**: The merge branch now combines quantificational goal formulas with multi-place predicate tracking and allows swapping between the enyan and LMH analyzer prompts; planning docs updated.
- **References**: `config/llm_task_analyzer_prompts_lmh.yaml`, `config/orchestrator_config.py`, `src/planning/pddl_domain_maintainer.py`, `src/planning/task_orchestrator.py`, `docs/planning.md`
